Los archivos `.mid` de esta carpeta son **MIDI GENERADO A PARTIR DE ANÁLISIS**
del Cuadernillo de Toques (JSON v4). No son MIDI oficiales de La Chilinga
ni transcripciones del disco.

Canal 10, TPQ=48, tempo y compás del JSON pedagógico (80–90 bpm).
Generados con `midi_from_v4.py`.
